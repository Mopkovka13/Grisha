package com.grinya.model;

/**
 * What is actually attached to an entry. Showcase blocks on the landing page
 * accept either — a photo skips the whole ffmpeg pipeline.
 */
public enum MediaType {
    VIDEO,
    IMAGE
}
